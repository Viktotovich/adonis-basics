<nav class="flex justify-between items-center">
  <div>
    <a class="mr-3" href="{{ route('home') }}">Home</a>
    <a class="mr-3" href="{{ route('directors.index') }}">Directors</a>
    <a class="mr-3" href="{{ route('writers.index') }}">Writers</a>
  </div>

  <div>
    @if (auth.user)
      <form method="POST" action="{{ route('auth.logout') }}">
        {{ csrfField() }}
        <button type="submit">Logout {{ auth.user.fullName }}</button>
      </form>
    @else
      <a class="mr-3" href="{{ route('auth.login.show') }}">Login</a>
      <a href="{{ route('auth.register.show') }}">Register</a>
    @endif
  </div>
</nav>
